ASSUME CS:CODE, DS:DATA
DATA SEGMENT
	ARR DW 42h, 10h, 30h
	COUNT DW 03H
	RESULT DW 1 DUP(?)
DATA ENDS
CODE SEGMENT
START:
	MOV AX, DATA
	MOV DS, AX
	XOR BX, BX
	MOV CX, COUNT
	LEA SI, ARR
NEXT:
	ADD BX, [SI]
	INC SI
	INC SI
	LOOP NEXT
	
	
	MOV RESULT, BX




	MOV AH, 4CH
	INT 21H
CODE ENDS
END START

